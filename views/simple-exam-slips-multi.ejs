<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Exam Student Slips - All Rooms</title>
    <style>
        @page {
            size: A4;
            margin: 5mm;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Arial', sans-serif;
            font-size: 9pt;
            line-height: 1.1;
            color: #000000;
            background: #ffffff;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
            padding: 0;
            margin: 0;
        }
        
        .room-section {
            page-break-before: always;
        }
        
        .room-section:first-child {
            page-break-before: avoid;
        }
        
        .page-container {
            width: 210mm;
            height: 297mm;
            margin: 0 auto;
            position: relative;
            page-break-inside: avoid;
            padding: 8mm 10mm;
            position: relative;
        }
        
        .room-header {
            text-align: center;
            margin-bottom: 3mm;
            padding-bottom: 1mm;
            border-bottom: 1px solid #ccc;
            font-size: 10pt;
            font-weight: bold;
            position: relative;
            z-index: 1;
        }
        
        .room-title {
            font-size: 11pt;
            margin-bottom: 0.5mm;
        }
        
        .room-stats {
            font-size: 8pt;
            color: #666;
        }
        
        /* Grid for 21 slips (7 rows x 3 columns) */
        .slips-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            grid-template-rows: repeat(7, 1fr);
            gap: 4mm;
            height: 100%;
            width: 100%;
        }
        
        /* Individual slip styling - SIMPLE AND CLEAN */
        .slip-container {
            border: 1px solid #000;
            padding: 3mm;
            position: relative;
            page-break-inside: avoid;
            break-inside: avoid;
            background: white;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            justify-content: center;
            height: 34.25mm;
            position: relative;
        }
        
        /* Watermark for every page */
        .watermark {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(-45deg);
            font-size: 70px;
            color: rgba(0, 0, 0, 0.1);
            font-weight: bold;
            pointer-events: none;
            z-index: -1;
            white-space: nowrap;
            opacity: 0.7;
        }
        
        /* Header - Centered */
        .slip-header {
            text-align: center;
            margin-bottom: 1.5mm;
            position: relative;
            z-index: 1;
        }
        
        .exam-name {
            font-size: 10pt;
            font-weight: bold;
            text-transform: uppercase;
            margin-bottom: 0.5mm;
            letter-spacing: 0.1px;
        }
        
        .school-name {
            font-size: 8pt;
            font-weight: bold;
            margin-bottom: .7mm;
        }
        
        /* Registration Number - Prominent */
        .reg-number {
            text-align: center;
            font-weight: bold;
            font-size: 14pt;
            margin: 1mm;
            padding: 0.5mm;
            border: 0.5px solid #000;
            position: relative;
            z-index: 1;
            background: white;
        }
        
        .reg-number span {
            font-size: 10pt;
        }
        
        /* Student Info - Simple */
        .student-info {
            margin-top: 1mm;
            position: relative;
            z-index: 1;
        }
        
        .student-name {
            font-size: 9pt;
            margin-bottom: 0.5mm;
        }
        
        .student-details {
            font-size: 8pt;
            color: #333;
            margin-top: 1mm;
        }
        
        .detail-row {
            margin-bottom: 0.3mm;
        }
        
        /* Divider line */
        .divider {
            height: 0.5px;
            background-color: #000;
            margin: 1mm 0;
        }
        
        /* Print Styles - FIX FOR RIGHT BORDER */
        @media print {
            body {
                margin: 0;
                padding: 0;
                background: white !important;
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
            }
            
            .page-container {
                margin: 0;
                padding: 0;
                width: 100%;
                height: 100%;
                page-break-after: always;
            }
            
            .slips-grid {
                width: 100%;
                height: 100%;
                padding: 8mm 10mm;
                box-sizing: border-box;
            }
            
            .slip-container {
                border: 1px solid #000 !important;
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
                box-shadow: none !important;
            }
            
            .room-header {
                border-bottom: 1px solid #ccc;
            }
            
            /* Ensure right border is visible in print */
            @page :first {
                margin: 5mm;
            }
            
            @page {
                margin: 5mm;
            }
            
            .no-print {
                display: none !important;
            }
        }
        
        /* Action Buttons (for preview) */
        .action-buttons {
            text-align: center;
            margin-top: 10mm;
            padding: 5mm;
            background-color: #f5f5f5;
            border-radius: 4px;
        }
        
        .print-button {
            background-color: #000;
            color: white;
            border: none;
            padding: 8px 20px;
            margin: 0 4px;
            border-radius: 3px;
            cursor: pointer;
            font-size: 11pt;
            font-weight: bold;
        }
        
        .back-button {
            background-color: #666;
            color: white;
            border: none;
            padding: 6px 15px;
            border-radius: 3px;
            cursor: pointer;
            font-size: 10pt;
            text-decoration: none;
            display: inline-block;
            margin-left: 8px;
        }
        
        /* Page break */
        .page-break {
            page-break-before: always;
        }
        
        /* Force visibility of borders in print */
        .slip-container {
            -webkit-box-shadow: inset 0 0 0 1px #000;
            -moz-box-shadow: inset 0 0 0 1px #000;
            box-shadow: inset 0 0 0 1px #000;
        }
        
        /* Empty slot styling removed since we don't need it */
        .empty-slot {
            display: none;
        }
    </style>
</head>
<body>
    <% roomNumbers.forEach(roomNo => { 
        const roomStudents = studentsByRoom[roomNo];
        if (!roomStudents || roomStudents.length === 0) return;
        
        // Split into pages of 21 (7x3 grid)
        const studentsPerPage = 21;
        const studentPages = [];
        for (let i = 0; i < roomStudents.length; i += studentsPerPage) {
            studentPages.push(roomStudents.slice(i, i + studentsPerPage));
        }
    %>
    
    <% studentPages.forEach((pageStudents, pageIndex) => { %>
        <div class="page-container <%= pageIndex > 0 || roomIndex > 0 ? 'page-break' : '' %>">
            <!-- Watermark -->
            <div class="watermark">PPMHSS KOTTUKKARA</div>
            
            <!-- Room Header -->
            <div class="room-header">
                <div class="room-title">Room <%= roomNo %></div>
                <div class="room-stats">
                    Total Students: <%= roomStudents.length %> | Page <%= pageIndex + 1 %> of <%= studentPages.length %>
                </div>
            </div>
            
            <!-- Slips Grid -->
            <div class="slips-grid">
                <% pageStudents.forEach((student) => { %>
                <div class="slip-container">
                    <!-- Header -->
                    <div class="slip-header">
                        <div class="exam-name">NMEA TENDER SCHOLAR 26</div>
                        <div class="school-name">PPMHSS KOTTUKKARA</div>
                    </div>
                    
                    <!-- Divider -->
                    <div class="divider"></div>
                    
                    <!-- Registration Number -->
                    <div class="reg-number">
                       <span>REG.No.:</span> <%= student.registrationCode %>
                    </div>
                    
                    <!-- Room Number -->
                    <div class="reg-number">
                       <span>Room.No.:</span> <%= student.roomNo %>
                    </div>
                </div>
                <% }) %>
            </div>
        </div>
    <% }) %>
    <% }) %>
    
    <!-- Action Buttons (Only for Preview) -->
    <% if (isPreview) { %>
    <div class="action-buttons no-print">
        <button class="print-button" onclick="window.print()">🖨️ Print All Slips</button>
        <a href="javascript:history.back()" class="back-button">← Back</a>
    </div>
    <% } %>
    
    <script>
        // Auto-print if specified
        <% if (autoPrint) { %>
        window.addEventListener('load', function() {
            setTimeout(function() {
                window.print();
            }, 500);
        });
        <% } %>
        
        // Add print functionality
        document.addEventListener('keydown', function(e) {
            if (e.ctrlKey && e.key === 'p') {
                e.preventDefault();
                window.print();
            }
        });
        
        // Force borders in print
        document.addEventListener('DOMContentLoaded', function() {
            const style = document.createElement('style');
            style.textContent = `
                @media print {
                    .slip-container {
                        -webkit-box-shadow: inset 0 0 0 1px #000 !important;
                        -moz-box-shadow: inset 0 0 0 1px #000 !important;
                        box-shadow: inset 0 0 0 1px #000 !important;
                    }
                    
                    body * {
                        -webkit-print-color-adjust: exact !important;
                        color-adjust: exact !important;
                    }
                }
            `;
            document.head.appendChild(style);
        });
    </script>
</body>
</html>