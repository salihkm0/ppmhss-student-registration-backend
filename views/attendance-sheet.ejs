<!-- Save this as views/attendance-sheet.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Attendance Sheet - Room <%= typeof roomNo !== 'undefined' ? roomNo : 'N/A' %></title>
    <style>
        @page {
            size: A4;
            margin: 10mm;
            border: 1px solid #000;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Arial', sans-serif;
            font-size: 10pt;
            line-height: 1.4;
            color: #000000;
            background: #ffffff;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
            padding: 0;
        }
        
        .page-container {
            width: 210mm;
            height: 287mm;
            margin: 5mm auto;
            position: relative;
            page-break-inside: avoid;
            border: 1px solid #000;
            padding: 8mm;
            overflow: hidden;
        }
        
        /* Header Section */
        .header-section {
            text-align: center;
            margin-bottom: 4mm;
            padding-bottom: 2mm;
            border-bottom: 1px solid #000;
        }
        
        .organization-name {
            font-size: 14pt;
            font-weight: bold;
            text-transform: uppercase;
            margin-bottom: 0.5mm;
            letter-spacing: 0.3px;
        }
        
        .school-name {
            font-size: 12pt;
            font-weight: bold;
            margin-bottom: 0.5mm;
        }
        
        .sheet-title {
            font-size: 14pt;
            font-weight: bold;
            margin: 2mm 0;
        }
        
        .room-info {
            font-size: 10pt;
            margin: 1mm 0;
        }
        
        /* Table Styles */
        .attendance-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 3mm;
            margin-bottom: 1mm;
            border: 1px solid #000;
            font-size: 8pt;
        }
        
        .attendance-table th {
            background-color: #f0f0f0;
            font-weight: bold;
            text-align: center;
            padding: 0.5mm 1mm;
            border: 0.5px solid #000;
            font-size: 8pt;
        }
        
        .attendance-table td {
            padding: 0.5mm 1mm;
            border: 0.5px solid #000;
            text-align: center;
            font-size: 8pt;
            height: 5mm;
        }
        
        .student-name-cell {
            text-align: left !important;
            padding-left: 1mm !important;
        }
        
        .aadhaar-cell {
            text-align: center !important;
            font-family: monospace;
            font-size: 8pt;
        }
        
        .medium-cell {
            text-align: center !important;
        }
        
        .qp-type-cell {
            text-align: center !important;
            font-weight: bold;
            background-color: transparent !important;
        }
        
        /* Empty row styling - completely transparent */
        .empty-row {
            background-color: transparent !important;
        }
        
        .empty-row td {
            background-color: transparent !important;
            color: transparent !important;
            border: 0.5px solid #000; /* Keep borders */
        }
        
        .empty-row .attendance-box,
        .empty-row .mark-box {
            display: none; /* Completely hide the boxes */
        }
        
        /* Column widths */
        .sl-no { width: 3%; }
        .reg-no { width: 8%; }
        .student-name { width: 20%; }
        .aadhaar-no { width: 12%; }
        .medium { width: 8%; }
        .qp-type { width: 5%; }
        .signature { width: 16%; }
        .attendance { width: 8%; }
        .mark { width: 8%; }
        
        .attendance-box {
            width: 8mm;
            height: 5mm;
            border: 0.5px solid #000;
            margin: 0 auto;
        }

        .mark-box {
            width: 10mm;
            height: 5mm;
            border: 0.5px solid #000;
            margin: 0 auto;
        }
        
        /* Invigilator Section */
        .invigilator-section {
            display: flex;
            justify-content: flex-end;
            width: 100%;
            margin-top: 2mm;
        }
        
        .invigilator-signature-line {
            width: 80mm;
            border-bottom: 1px solid #000;
            text-align: center;
            margin-top: 2mm;
        }
        
        /* Footer */
        .footer-section {
            position: absolute;
            bottom: 3mm;
            left: 8mm;
            right: 8mm;
            font-size: 7pt;
            text-align: center;
            color: #555;
            padding-top: 2mm;
            border-top: 0.5px dashed #000;
        }
        
        .footer-info {
            display: flex;
            justify-content: space-between;
            margin-bottom: 0.5mm;
        }
        
        /* Print Styles */
        @media print {
            body {
                margin: 0;
                padding: 0;
                background: white !important;
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
            }
            
            .page-container {
                margin: 0;
                box-shadow: none !important;
                height: 287mm;
                width: 210mm;
                border: none;
            }
            
            .no-print {
                display: none !important;
            }
            
            .attendance-table th {
                background-color: #f0f0f0 !important;
            }
            
            .qp-type-cell {
                background-color: transparent !important;
            }
            
            .empty-row {
                background-color: transparent !important;
            }
            
            .empty-row td {
                background-color: transparent !important;
                color: transparent !important;
            }
        }
        
        /* Action Buttons */
        .action-buttons {
            text-align: center;
            margin: 20px;
            padding: 20px;
            background-color: #f5f5f5;
            border-radius: 4px;
        }
        
        .print-button {
            background-color: #000;
            color: white;
            border: none;
            padding: 10px 30px;
            margin: 0 10px;
            border-radius: 3px;
            cursor: pointer;
            font-size: 14px;
            font-weight: bold;
        }
        
        .back-button {
            background-color: #666;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 3px;
            cursor: pointer;
            font-size: 14px;
            text-decoration: none;
            display: inline-block;
        }
        
        .page-break {
            page-break-before: always;
        }
        
        /* Watermark */
        .watermark {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(-45deg);
            font-size: 60px;
            color: rgba(0, 0, 0, 0.05);
            font-weight: bold;
            pointer-events: none;
            z-index: -1;
            white-space: nowrap;
            opacity: 0.7;
        }
        
        .table-container {
            height: calc(100% - 90mm);
            overflow: hidden;
        }
        
        .invigilator-container {
            display: flex;
            justify-content: flex-end;
            margin-right: 1mm;
        }
        
        .invigilator-right {
            width: auto;
        }
        
        .invigilator-label {
            font-weight: bold;
            width: 100mm;
        }
        
        .no-data {
            text-align: center;
            margin-top: 50mm;
        }
        
        .no-data h2 {
            color: #666;
            margin-bottom: 20px;
        }
        
        .qp-legend {
            display: flex;
            justify-content: flex-start;
            gap: 20px;
            margin-top: 2mm;
            font-size: 8pt;
        }
        
        .legend-item {
            display: flex;
            align-items: center;
            gap: 5px;
        }
        
        .legend-box {
            width: 12px;
            height: 12px;
            border: 1px solid #000;
            background-color: transparent !important;
        }
    </style>
</head>
<body>
    <%
        // Initialize variables with safe defaults
        const safeRoomNo = typeof roomNo !== 'undefined' ? roomNo : 'N/A';
        const safeExamDate = typeof examDate !== 'undefined' ? examDate : new Date().toLocaleDateString('en-IN');
        const safeExamTime = typeof examTime !== 'undefined' ? examTime : '10:00 AM - 11:30 PM';
        const safeGenerationDate = typeof generationDate !== 'undefined' ? generationDate : new Date().toLocaleDateString('en-IN');
        const safeTotalStudents = typeof totalStudents !== 'undefined' ? totalStudents : 0;
        const safeStudentPages = typeof studentPages !== 'undefined' ? studentPages : [];
        const safeIsPreview = typeof isPreview !== 'undefined' ? isPreview : false;
        const safeAutoPrint = typeof autoPrint !== 'undefined' ? autoPrint : false;
        
        // Function to determine question paper type (alternating A/B based on index)
        function getQuestionPaperType(index) {
            return (index % 2 === 0) ? 'A' : 'B';
        }
        
        // Calculate total students across all pages for consistent alternation
        let globalIndex = 0;
    %>
    
    <% if (safeStudentPages && safeStudentPages.length > 0) { %>
        <% safeStudentPages.forEach((pageStudents, pageIndex) => { %>
            <div class="page-container <%= pageIndex > 0 ? 'page-break' : '' %>">
                <!-- Watermark for every page -->
                <div class="watermark">PPMHSS KOTTUKKARA</div>
                
                <!-- Header -->
                <div class="header-section">
                    <div class="organization-name">NMEA TENDER SCHOLAR 26</div>
                    <div class="school-name">PPMHSS KOTTUKKARA</div>
                    <div class="sheet-title">ATTENDANCE SHEET</div>
                    <div class="room-info">
                        Room No: <strong><%= safeRoomNo %></strong> | 
                        Date: <strong><%= safeExamDate %></strong> | 
                        Time: <strong><%= safeExamTime %></strong>
                    </div>
                    
                    <!-- Question Paper Type Legend - Black and White -->
                    <div class="qp-legend">
                        <div class="legend-item">
                            <div class="legend-box"></div>
                            <span>Question Paper Type A</span>
                        </div>
                        <div class="legend-item">
                            <div class="legend-box"></div>
                            <span>Question Paper Type B</span>
                        </div>
                    </div>
                </div>

                <!-- Attendance Table -->
                <div class="table-container">
                    <table class="attendance-table">
                        <thead>
                            <tr>
                                <th class="sl-no">SL No</th>
                                <th class="reg-no">Reg No</th>
                                <th class="student-name">Candidate Name</th>
                                <th class="aadhaar-no">Aadhaar No</th>
                                <th class="medium">Medium</th>
                                <th class="qp-type">QP Type</th>
                                <th class="signature">Signature</th>
                                <th class="attendance">P/A</th>
                                <th class="mark">Mark</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% pageStudents.forEach((student, index) => { 
                                const slNo = pageIndex * 20 + index + 1;
                                const qpType = getQuestionPaperType(globalIndex);
                                globalIndex++; // Increment global index for next student
                                
                                // Format Aadhaar number to show last 6 digits only
                                let displayAadhaar = '';
                                if (student.aadhaarNo) {
                                    const aadhaar = student.aadhaarNo.toString();
                                    displayAadhaar = aadhaar.slice(-6);
                                }
                            %>
                            <tr>
                                <td class="sl-no"><%= slNo %></td>
                                <td class="reg-no"><%= student.registrationCode || '' %></td>
                                <td class="student-name student-name-cell">
                                    <%= student.name || '' %>
                                </td>
                                <td class="aadhaar-no aadhaar-cell">
                                    <%= displayAadhaar %>
                                </td>
                                <td class="medium medium-cell">
                                    <%= student.medium ? student.medium : '-' %>
                                </td>
                                <td class="qp-type qp-type-cell">
                                    <strong><%= qpType %></strong>
                                </td>
                                <td class="signature">
                                    <!-- Empty space for signature -->
                                </td>
                                <td class="attendance">
                                    <div class="attendance-box"></div>
                                </td>
                                <td class="mark">
                                    <div class="mark-box"></div>
                                </td>
                            </tr>
                            <% }) %>
                            
                            <!-- Fill remaining rows if less than 20 - COMPLETELY TRANSPARENT -->
                            <% if (pageStudents.length < 20) { %>
                                <% for (let i = pageStudents.length; i < 20; i++) { 
                                    // Still increment globalIndex to maintain sequence
                                    globalIndex++;
                                %>
                                <tr class="empty-row">
                                    <td class="sl-no"><%= pageIndex * 20 + i + 1 %></td>
                                    <td class="reg-no"></td>
                                    <td class="student-name student-name-cell"></td>
                                    <td class="aadhaar-no aadhaar-cell"></td>
                                    <td class="medium medium-cell"></td>
                                    <td class="qp-type qp-type-cell"></td>
                                    <td class="signature"></td>
                                    <td class="attendance"></td>
                                    <td class="mark"></td>
                                </tr>
                                <% } %>
                            <% } %>
                        </tbody>
                    </table>
                </div>

                <!-- Invigilator Section -->
                <div class="invigilator-section">
                    <div class="invigilator-container">
                        <div class="invigilator-right">
                            <div class="invigilator-label">Invigilator Name & Signature:</div>
                            <div class="invigilator-signature-line"></div>
                        </div>
                    </div>
                </div>

                <!-- Footer -->
                <div class="footer-section">
                    <div class="footer-info">
                        <div>Page: <strong><%= pageIndex + 1 %> of <%= safeStudentPages.length %></strong></div>
                        <div>Total Students: <strong><%= safeTotalStudents %></strong></div>
                        <div>Generated: <%= safeGenerationDate %></div>
                    </div>
                    <div style="margin-top: 1mm; font-style: italic;">
                        Note: 'P' for Present, 'A' for Absent. QP Type A/B alternates by seat number.
                        Invigilators must sign after verifying attendance.
                    </div>
                </div>
            </div>
        <% }) %>
    <% } else { %>
        <div class="page-container">
            <div class="no-data">
                <h2>No students found in this room</h2>
                <p>Room No: <strong><%= safeRoomNo %></strong></p>
                <a href="javascript:history.back()" class="back-button">← Back</a>
            </div>
        </div>
    <% } %>
    
    <!-- Action Buttons (Only for Preview) -->
    <% if (safeIsPreview) { %>
    <div class="action-buttons no-print">
        <button class="print-button" onclick="window.print()">🖨️ Print Attendance Sheet</button>
        <a href="javascript:history.back()" class="back-button">← Back</a>
    </div>
    <% } %>
    
    <script>
        // Auto-print if specified
        <% if (safeAutoPrint) { %>
        window.addEventListener('load', function() {
            setTimeout(function() {
                window.print();
            }, 500);
        });
        <% } %>
        
        // Add print functionality
        document.addEventListener('keydown', function(e) {
            if (e.ctrlKey && e.key === 'p') {
                e.preventDefault();
                window.print();
            }
        });
    </script>
</body>
</html>