<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Attendance Sheet - Room <%= typeof roomNo !== 'undefined' ? roomNo : 'N/A' %></title>
    <style>
        @page {
            size: A4;
            margin: 10mm; /* Reduced margin */
            border: 1px solid #000; /* Added border to page */
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Arial', sans-serif;
            font-size: 10pt;
            line-height: 1.4; /* Reduced line height */
            color: #000000;
            background: #ffffff;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
            padding: 0;
        }
        
        .page-container {
            width: 210mm;
            height: 287mm; /* Fixed height to fit A4 */
            margin: 5mm auto; /* Reduced margin */
            position: relative;
            page-break-inside: avoid;
            border: 1px solid #000;
            padding: 8mm; /* Reduced padding */
            overflow: hidden; /* Prevent overflow */
        }
        
        /* Header Section - More compact */
        .header-section {
            text-align: center;
            margin-bottom: 4mm; /* Reduced */
            padding-bottom: 2mm; /* Reduced */
            border-bottom: 1px solid #000; /* Thinner border */
        }
        
        .organization-name {
            font-size: 14pt; /* Smaller */
            font-weight: bold;
            text-transform: uppercase;
            margin-bottom: 0.5mm; /* Reduced */
            letter-spacing: 0.3px;
        }
        
        .school-name {
            font-size: 12pt; /* Smaller */
            font-weight: bold;
            margin-bottom: 0.5mm; /* Reduced */
        }
        
        .sheet-title {
            font-size: 14pt; /* Smaller */
            font-weight: bold;
            margin: 2mm 0; /* Reduced */
        }
        
        .room-info {
            font-size: 10pt; /* Smaller */
            margin: 1mm 0; /* Reduced */
        }
        
        /* Table Styles - More compact */
        .attendance-table {
            width: 100%;
            border-collapse: collapse;
            margin: 3mm 0; /* Reduced */
            border: 1px solid #000;
            font-size: 8pt; /* Smaller font */
        }
        
        .attendance-table th {
            background-color: #f0f0f0;
            font-weight: bold;
            text-align: center;
            padding: 0.5mm 1mm; /* Reduced padding */
            border: 0.5px solid #000; /* Thinner borders */
            font-size: 8pt;
        }
        
        .attendance-table td {
            padding: 0.5mm 1mm; /* Reduced padding */
            border: 0.5px solid #000; /* Thinner borders */
            text-align: center;
            font-size: 8pt;
            height: 5mm; /* Fixed row height */
        }
        
        .student-name-cell {
            text-align: left !important;
            padding-left: 1mm !important;
        }
        
        .father-name-cell {
            text-align: left !important;
            padding-left: 1mm !important;
        }
        
        /* Adjust column widths */
        .sl-no { width: 4%; }
        .reg-no { width: 12%; }
        .student-name { width: 30%; }
        .father-name { width: 18%; } /* Changed from class to father-name */
        .signature { width: 18%; }
        .attendance { width: 18%; }
        
        .attendance-box {
            width: 10mm;
            height: 8mm;
            border: 0.5px solid #000;
            margin: 0 auto;
        }
        
        /* Invigilator Signature Section */
        .invigilator-section {
            margin-top: 8mm;
            padding-top: 3mm;
            border-top: 1px dashed #000;
            font-size: 9pt;
        }
        
        .invigilator-signature-line {
            width: 60mm;
            border-top: 1px solid #000;
            margin-top: 15mm;
            text-align: center;
            padding-top: 1mm;
        }
        
        /* Footer - More compact */
        .footer-section {
            position: absolute;
            bottom: 5mm;
            left: 8mm;
            right: 8mm;
            font-size: 7pt; /* Smaller */
            text-align: center;
            color: #555;
            padding-top: 2mm;
            border-top: 0.5px dashed #000;
        }
        
        .footer-info {
            display: flex;
            justify-content: space-between;
            margin-bottom: 0.5mm;
        }
        
        /* Print Styles */
        @media print {
            body {
                margin: 0;
                padding: 0;
                background: white !important;
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
            }
            
            .page-container {
                margin: 0;
                box-shadow: none !important;
                height: 287mm;
                width: 210mm;
                border: none;
            }
            
            .no-print {
                display: none !important;
            }
            
            .attendance-table th {
                background-color: #f0f0f0 !important;
            }
        }
        
        /* Action Buttons (for preview) */
        .action-buttons {
            text-align: center;
            margin-top: 10mm;
            padding: 5mm;
            background-color: #f5f5f5;
            border-radius: 4px;
        }
        
        .print-button {
            background-color: #000;
            color: white;
            border: none;
            padding: 8px 20px;
            margin: 0 4px;
            border-radius: 3px;
            cursor: pointer;
            font-size: 11pt;
            font-weight: bold;
        }
        
        .back-button {
            background-color: #666;
            color: white;
            border: none;
            padding: 6px 15px;
            border-radius: 3px;
            cursor: pointer;
            font-size: 10pt;
            text-decoration: none;
            display: inline-block;
            margin-left: 8px;
        }
        
        /* Page break for multiple pages */
        .page-break {
            page-break-before: always;
        }
        
        /* Watermark for every page */
        .watermark {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(-45deg);
            font-size: 60px; /* Smaller */
            color: rgba(0, 0, 0, 0.05); /* Slightly darker for better visibility */
            font-weight: bold;
            pointer-events: none;
            z-index: -1;
            white-space: nowrap;
            opacity: 0.7;
        }
        
        /* Ensure 20 rows fit exactly */
        .table-container {
            height: calc(100% - 70mm); /* Adjusted for header, invigilator section, and footer */
            overflow: hidden;
        }
        
        /* Invigilator details container */
        .invigilator-container {
            display: flex;
            justify-content: space-between;
            margin-top: 5mm;
        }
        
        .invigilator-left, .invigilator-right {
            width: 45%;
        }
        
        .invigilator-label {
            font-weight: bold;
            margin-bottom: 2mm;
        }
    </style>
</head>
<body>
    <% 
        // Initialize variables with safe defaults
        const safeRoomNo = typeof roomNo !== 'undefined' ? roomNo : 'N/A';
        const safeExamDate = typeof examDate !== 'undefined' ? examDate : new Date().toLocaleDateString('en-IN');
        const safeExamTime = typeof examTime !== 'undefined' ? examTime : '10:00 AM - 11:30 PM';
        const safeGenerationDate = typeof generationDate !== 'undefined' ? generationDate : new Date().toLocaleDateString('en-IN');
        const safeTotalStudents = typeof totalStudents !== 'undefined' ? totalStudents : 0;
        const safeStudentPages = typeof studentPages !== 'undefined' ? studentPages : [];
        const safeIsPreview = typeof isPreview !== 'undefined' ? isPreview : false;
        const safeAutoPrint = typeof autoPrint !== 'undefined' ? autoPrint : false;
    %>
    
    <% safeStudentPages.forEach((pageStudents, pageIndex) => { %>
        <div class="page-container <%= pageIndex > 0 ? 'page-break' : '' %>">
            <!-- Watermark for every page -->
            <div class="watermark">PPMHSS KOTTUKKARA</div>
            
            <!-- Header -->
            <div class="header-section">
                <div class="organization-name">NMEA TENDER SCHOLAR 26</div>
                <div class="school-name">PPMHSS KOTTUKKARA</div>
                <div class="sheet-title">ATTENDANCE SHEET</div>
                <div class="room-info">
                    Room No: <strong><%= safeRoomNo %></strong> | 
                    Date: <strong><%= safeExamDate %></strong> | 
                    Time: <strong><%= safeExamTime %></strong>
                </div>
            </div>

            <!-- Attendance Table -->
            <div class="table-container">
                <table class="attendance-table">
                    <thead>
                        <tr>
                            <th class="sl-no">SL No</th>
                            <th class="reg-no">Registration No</th>
                            <th class="student-name">Name of Candidate</th>
                            <th class="father-name">Father's Name</th> <!-- Changed from Class to Father's Name -->
                            <th class="signature">Candidate's Signature</th>
                            <th class="attendance">Attendance (P/A)</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% pageStudents.forEach((student, index) => { 
                            const slNo = pageIndex * 20 + index + 1;
                        %>
                        <tr>
                            <td class="sl-no"><%= slNo %></td>
                            <td class="reg-no"><%= student.registrationCode %></td>
                            <td class="student-name student-name-cell">
                                <%= student.name %>
                            </td>
                            <td class="father-name father-name-cell">
                                <%= student.fatherName %> <!-- Changed from studyingClass to fatherName -->
                            </td>
                            <td class="signature">
                                <!-- Empty space for signature -->
                            </td>
                            <td class="attendance">
                                <div class="attendance-box"></div>
                            </td>
                        </tr>
                        <% }) %>
                        
                        <!-- Fill remaining rows if less than 20 -->
                        <% if (pageStudents.length < 20) { %>
                            <% for (let i = pageStudents.length; i < 20; i++) { %>
                            <tr>
                                <td class="sl-no"><%= pageIndex * 20 + i + 1 %></td>
                                <td class="reg-no"></td>
                                <td class="student-name student-name-cell"></td>
                                <td class="father-name father-name-cell"></td>
                                <td class="signature"></td>
                                <td class="attendance">
                                    <div class="attendance-box"></div>
                                </td>
                            </tr>
                            <% } %>
                        <% } %>
                    </tbody>
                </table>
                
                <!-- Invigilator Signature Section -->
                <div class="invigilator-section">
                    <div class="invigilator-container">
                        <div class="invigilator-left">
                            <div class="invigilator-label">Invigilator 1:</div>
                            <div class="invigilator-signature-line">
                                Signature
                            </div>
                            <div style="margin-top: 2mm; font-size: 8pt;">
                                Name: ____________________
                            </div>
                        </div>
                        
                        <div class="invigilator-right">
                            <div class="invigilator-label">Invigilator 2:</div>
                            <div class="invigilator-signature-line">
                                Signature
                            </div>
                            <div style="margin-top: 2mm; font-size: 8pt;">
                                Name: ____________________
                            </div>
                        </div>
                    </div>
                    
                    <!-- Additional space for more signatures if needed -->
                    <div style="margin-top: 10mm; font-size: 8pt; text-align: center;">
                        <div style="margin-bottom: 3mm;">
                            <strong>Chief Invigilator/Exam Superintendent:</strong>
                        </div>
                        <div class="invigilator-signature-line" style="width: 80mm; margin: 0 auto;">
                            Signature & Seal
                        </div>
                    </div>
                </div>
            </div>

            <!-- Footer - Fixed at bottom -->
            <div class="footer-section">
                <div class="footer-info">
                    <div>Page: <strong><%= pageIndex + 1 %> of <%= safeStudentPages.length %></strong></div>
                    <div>Total Students: <strong><%= safeTotalStudents %></strong></div>
                    <div>Generated: <%= safeGenerationDate %></div>
                </div>
                <div style="margin-top: 1mm; font-style: italic;">
                    Note: 'P' for Present, 'A' for Absent. Invigilators must sign after verifying attendance.
                </div>
            </div>
        </div>
    <% }) %>
    
    <!-- Action Buttons (Only for Preview) -->
    <% if (safeIsPreview) { %>
    <div class="action-buttons no-print">
        <button class="print-button" onclick="window.print()">🖨️ Print Attendance Sheet</button>
        <a href="javascript:history.back()" class="back-button">← Back</a>
    </div>
    <% } %>
    
    <script>
        // Auto-print if specified
        <% if (safeAutoPrint) { %>
        window.addEventListener('load', function() {
            setTimeout(function() {
                window.print();
            }, 500);
        });
        <% } %>
        
        // Add print functionality
        document.addEventListener('keydown', function(e) {
            if (e.ctrlKey && e.key === 'p') {
                e.preventDefault();
                window.print();
            }
        });
    </script>
</body>
</html>