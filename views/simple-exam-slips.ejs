<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Exam Student Slips - Room <%= roomNo %></title>
    <style>
        @page {
            size: A4;
            margin: 5mm;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Arial', sans-serif;
            font-size: 9pt;
            line-height: 1.1;
            color: #000000;
            background: #ffffff;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
            padding: 0;
            margin: 0;
        }
        
        .page-container {
            width: 210mm;
            height: 297mm;
            margin: 0 auto;
            position: relative;
            page-break-inside: avoid;
            padding: 8mm 10mm;
            position: relative;
            border: 1px solid #000;
        }
        
        /* Grid for 21 slips (7 rows x 3 columns) */
        .slips-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            grid-template-rows: repeat(7, 1fr);
            gap: 4mm;
            height: 100%;
            width: 100%;
        }
        
        /* Individual slip styling */
        .slip-container {
            border: 1px solid #000;
            padding: 2mm;
            position: relative;
            page-break-inside: avoid;
            break-inside: avoid;
            background: white;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            height: 34.25mm;
            position: relative;
        }
        
        /* Header */
        .slip-header {
            text-align: center;
            margin-bottom: 0.5mm;
        }
        
        .exam-name {
            font-size: 10pt;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 0.1px;
            line-height: 1.2;
        }
        
        .school-name {
            font-size: 9pt;
            font-weight: bold;
            margin-top: 0.5mm;
        }
        
        /* Boxed items */
        .reg-box {
            border: 1px solid #000;
            margin: 1mm 0;
            padding: 1mm 0;
            text-align: center;
            font-weight: bold;
            font-size: 12pt;
        }
        
        .reg-box span {
            font-size: 9pt;
            font-weight: normal;
        }
        
        /* Three-column layout for bottom info */
        .bottom-info {
            display: flex;
            margin-top: 1mm;
            gap: 1mm;
        }
        
        .info-item {
            flex: 1;
            border: 1px solid #000;
            text-align: center;
            padding: 0.5mm 0;
            font-weight: bold;
            background: white;
        }
        
        .info-item .label {
            font-size: 7pt;
            font-weight: normal;
            display: block;
            color: #333;
        }
        
        .info-item .value {
            font-size: 10pt;
            font-weight: bold;
        }
        
        /* Room box - smaller size */
        .room-box {
            border: 1px solid #000;
            margin: 1mm 0;
            padding: 0.5mm 0;
            text-align: center;
            font-weight: bold;
            font-size: 10pt; /* Reduced from 12pt to 10pt */
        }
        
        .room-box span {
            font-size: 8pt;
            font-weight: normal;
        }
        
        /* Print Styles */
        @media print {
            body {
                margin: 0;
                padding: 0;
                background: white !important;
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
            }
            
            .page-container {
                margin: 0;
                padding: 8mm 10mm;
                width: 100%;
                height: 100%;
                page-break-after: always;
                border: none;
            }
            
            .slips-grid {
                width: 100%;
                height: 100%;
            }
            
            .slip-container {
                border: 1px solid #000 !important;
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
            }
            
            .reg-box, .room-box, .info-item {
                border: 1px solid #000 !important;
            }
            
            @page {
                margin: 5mm;
            }
            
            .no-print {
                display: none !important;
            }
        }
        
        /* Action Buttons */
        .action-buttons {
            text-align: center;
            margin-top: 10mm;
            padding: 5mm;
            background-color: #f5f5f5;
            border-radius: 4px;
        }
        
        .print-button {
            background-color: #000;
            color: white;
            border: none;
            padding: 8px 20px;
            margin: 0 4px;
            border-radius: 3px;
            cursor: pointer;
            font-size: 11pt;
            font-weight: bold;
        }
        
        .back-button {
            background-color: #666;
            color: white;
            border: none;
            padding: 6px 15px;
            border-radius: 3px;
            cursor: pointer;
            font-size: 10pt;
            text-decoration: none;
            display: inline-block;
            margin-left: 8px;
        }
        
        .page-break {
            page-break-before: always;
        }
    </style>
</head>
<body>
    <% studentPages.forEach((pageStudents, pageIndex) => { %>
        <div class="page-container <%= pageIndex > 0 ? 'page-break' : '' %>">
            <div class="slips-grid">
                <% pageStudents.forEach((student) => { %>
                <div class="slip-container">
                    <!-- Header -->
                    <div class="slip-header">
                        <div class="exam-name">NMEA TENDER SCHOLAR 26</div>
                        <!-- <div class="school-name">PPM HSS KOTTUKKARA</div> -->
                    </div>
                    
                    <!-- Registration Number -->
                    <div class="reg-box">
                        REG.No.: <%= student.registrationCode %>
                    </div>

                    <!-- Medium and QP Type in boxes -->
                    <div class="bottom-info">
                        <div class="info-item">
                            <span class="label">Medium</span>
                            <span class="value"><%= student.medium || '' %></span>
                        </div>
                        <div class="info-item">
                            <span class="label">QP Type</span>
                            <span class="value"><%= student.qpType || '' %></span>
                        </div>
                    </div>
                    
                    <!-- Room Number - smaller now -->
                    <div class="room-box">
                        Room : <%= roomNo %>
                    </div>
                    
                    
                </div>
                <% }) %>
            </div>
        </div>
    <% }) %>
    
    <!-- Action Buttons -->
    <% if (isPreview) { %>
    <div class="action-buttons no-print">
        <button class="print-button" onclick="window.print()">🖨️ Print All Slips</button>
        <button class="print-button" onclick="printCurrentPage()">🖨️ Print Current Page</button>
        <a href="javascript:history.back()" class="back-button">← Back</a>
    </div>
    <% } %>
    
    <script>
        <% if (autoPrint) { %>
        window.addEventListener('load', function() {
            setTimeout(function() {
                window.print();
            }, 500);
        });
        <% } %>
        
        document.addEventListener('keydown', function(e) {
            if (e.ctrlKey && e.key === 'p') {
                e.preventDefault();
                window.print();
            }
        });
        
        function printCurrentPage() {
            const originalBody = document.body.innerHTML;
            const pageContent = document.querySelector('.page-container').outerHTML;
            
            document.body.innerHTML = pageContent;
            window.print();
            document.body.innerHTML = originalBody;
            window.location.reload();
        }
    </script>
</body>
</html>