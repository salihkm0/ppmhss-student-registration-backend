<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Overall Summary - All Students</title>
    <style>
        @page {
            size: A4;
            margin: 10mm;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Arial', sans-serif;
            font-size: 10pt;
            line-height: 1.4;
            color: #000000;
            background: #ffffff;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
            padding: 0;
        }
        
        .page-container {
            width: 210mm;
            min-height: 287mm;
            margin: 5mm auto;
            position: relative;
            page-break-inside: avoid;
            border: 1px solid #000;
            padding: 8mm;
        }
        
        /* Header Section */
        .header-section {
            text-align: center;
            margin-bottom: 6mm;
            padding-bottom: 3mm;
            border-bottom: 2px solid #000;
        }
        
        .organization-name {
            font-size: 18pt;
            font-weight: bold;
            text-transform: uppercase;
            margin-bottom: 1mm;
            letter-spacing: 0.5px;
        }
        
        .school-name {
            font-size: 14pt;
            font-weight: bold;
            margin-bottom: 1mm;
        }
        
        .report-title {
            font-size: 16pt;
            font-weight: bold;
            margin: 3mm 0;
            text-decoration: underline;
        }
        
        .generation-info {
            font-size: 10pt;
            margin: 2mm 0;
            color: #333;
        }
        
        /* Summary Cards */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 3mm;
            margin-bottom: 6mm;
        }
        
        .stat-card {
            border: 1px solid #000;
            padding: 2mm;
            text-align: center;
            background-color: #f9f9f9;
        }
        
        .stat-label {
            font-size: 9pt;
            font-weight: normal;
            color: #555;
            margin-bottom: 1mm;
        }
        
        .stat-value {
            font-size: 18pt;
            font-weight: bold;
        }
        
        .stat-sub {
            font-size: 8pt;
            color: #666;
            margin-top: 1mm;
        }
        
        /* Table Styles */
        .section-title {
            font-size: 14pt;
            font-weight: bold;
            margin: 5mm 0 3mm 0;
            padding-bottom: 1mm;
            border-bottom: 1px solid #000;
        }
        
        .summary-table {
            width: 100%;
            border-collapse: collapse;
            border: 1px solid #000;
            font-size: 10pt;
            margin-bottom: 5mm;
        }
        
        .summary-table th {
            background-color: #f0f0f0;
            font-weight: bold;
            text-align: center;
            padding: 1.5mm;
            border: 1px solid #000;
        }
        
        .summary-table td {
            padding: 1.5mm;
            border: 1px solid #000;
            text-align: center;
        }
        
        .summary-table .total-row {
            font-weight: bold;
            background-color: #f9f9f9;
        }
        
        .summary-table .section-header {
            background-color: #e0e0e0;
            font-weight: bold;
        }
        
        .summary-table .medium-label {
            font-weight: bold;
            text-align: left;
            padding-left: 3mm;
        }
        
        .room-table {
            width: 100%;
            border-collapse: collapse;
            border: 1px solid #000;
            font-size: 9pt;
            margin-bottom: 5mm;
        }
        
        .room-table th {
            background-color: #f0f0f0;
            font-weight: bold;
            text-align: center;
            padding: 1mm;
            border: 1px solid #000;
        }
        
        .room-table td {
            padding: 1mm;
            border: 1px solid #000;
            text-align: center;
        }
        
        .room-table .total-row {
            font-weight: bold;
            background-color: #f9f9f9;
        }
        
        .qp-breakdown {
            font-size: 8pt;
            color: #555;
            margin-top: 2px;
        }
        
        /* Footer */
        .footer-section {
            margin-top: 8mm;
            padding-top: 2mm;
            border-top: 1px dashed #000;
            font-size: 8pt;
            text-align: center;
            color: #555;
        }
        
        /* Print Styles */
        @media print {
            body {
                margin: 0;
                padding: 0;
                background: white !important;
            }
            
            .page-container {
                margin: 0;
                box-shadow: none !important;
                border: none;
            }
            
            .no-print {
                display: none !important;
            }
            
            .summary-table th,
            .room-table th {
                background-color: #f0f0f0 !important;
            }
            
            .stat-card {
                background-color: #f9f9f9 !important;
            }
        }
        
        /* Action Buttons */
        .action-buttons {
            text-align: center;
            margin: 20px;
            padding: 20px;
            background-color: #f5f5f5;
            border-radius: 4px;
        }
        
        .print-button {
            background-color: #000;
            color: white;
            border: none;
            padding: 10px 30px;
            margin: 0 10px;
            border-radius: 3px;
            cursor: pointer;
            font-size: 14px;
            font-weight: bold;
        }
        
        .back-button {
            background-color: #666;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 3px;
            cursor: pointer;
            font-size: 14px;
            text-decoration: none;
            display: inline-block;
        }
        
        .watermark {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(-45deg);
            font-size: 80px;
            color: rgba(0, 0, 0, 0.03);
            font-weight: bold;
            pointer-events: none;
            z-index: -1;
            white-space: nowrap;
        }
    </style>
</head>
<body>
    <%
        const safeSummary = typeof summary !== 'undefined' ? summary : {};
        const safeRooms = typeof rooms !== 'undefined' ? rooms : [];
        const safeTotalStudents = typeof totalStudents !== 'undefined' ? totalStudents : 0;
        const safeTotalRooms = typeof totalRooms !== 'undefined' ? totalRooms : 0;
        const safeGenerationDate = typeof generationDate !== 'undefined' ? generationDate : new Date().toLocaleDateString('en-IN');
        const safeGenerationTime = typeof generationTime !== 'undefined' ? generationTime : new Date().toLocaleTimeString('en-IN');
    %>
    
    <div class="page-container">
        <!-- Watermark -->
        <div class="watermark">PPMHSS KOTTUKKARA</div>
        
        <!-- Header -->
        <div class="header-section">
            <div class="organization-name">NMEA TENDER SCHOLAR 26</div>
            <div class="school-name">PPMHSS KOTTUKKARA</div>
            <div class="report-title">OVERALL SUMMARY REPORT</div>
            <div class="generation-info">
                Generated on: <strong><%= safeGenerationDate %> at <%= safeGenerationTime %></strong> | 
                Total Students: <strong><%= safeTotalStudents %></strong> | 
                Total Rooms: <strong><%= safeTotalRooms %></strong>
            </div>
        </div>

        <!-- Stats Cards -->
        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-label">Total Students</div>
                <div class="stat-value"><%= safeTotalStudents %></div>
                <div class="stat-sub">Across <%= safeTotalRooms %> Rooms</div>
            </div>
            <div class="stat-card">
                <div class="stat-label">English Medium</div>
                <div class="stat-value"><%= safeSummary.totalEnglishStudents || 0 %></div>
                <div class="stat-sub">Male: <%= safeSummary.englishMale || 0 %>, Female: <%= safeSummary.englishFemale || 0 %></div>
            </div>
            <div class="stat-card">
                <div class="stat-label">Malayalam Medium</div>
                <div class="stat-value"><%= safeSummary.totalMalayalamStudents || 0 %></div>
                <div class="stat-sub">Male: <%= safeSummary.malayalamMale || 0 %>, Female: <%= safeSummary.malayalamFemale || 0 %></div>
            </div>
            <div class="stat-card">
                <div class="stat-label">Question Papers</div>
                <div class="stat-value">A: <%= safeSummary.totalTypeA || 0 %></div>
                <div class="stat-sub">B: <%= safeSummary.totalTypeB || 0 %></div>
            </div>
        </div>

        <!-- Gender by Medium -->
        <div class="section-title">GENDER DISTRIBUTION BY MEDIUM</div>
        <table class="summary-table">
            <thead>
                <tr>
                    <th>Medium</th>
                    <th>Male</th>
                    <th>Female</th>
                    <th>Other</th>
                    <th>Total</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td class="medium-label">English</td>
                    <td><%= safeSummary.englishMale || 0 %></td>
                    <td><%= safeSummary.englishFemale || 0 %></td>
                    <td><%= safeSummary.englishOther || 0 %></td>
                    <td><%= safeSummary.totalEnglishStudents || 0 %></td>
                </tr>
                <tr>
                    <td class="medium-label">Malayalam</td>
                    <td><%= safeSummary.malayalamMale || 0 %></td>
                    <td><%= safeSummary.malayalamFemale || 0 %></td>
                    <td><%= safeSummary.malayalamOther || 0 %></td>
                    <td><%= safeSummary.totalMalayalamStudents || 0 %></td>
                </tr>
                <tr class="total-row">
                    <td class="medium-label">TOTAL</td>
                    <td><%= (safeSummary.englishMale || 0) + (safeSummary.malayalamMale || 0) %></td>
                    <td><%= (safeSummary.englishFemale || 0) + (safeSummary.malayalamFemale || 0) %></td>
                    <td><%= (safeSummary.englishOther || 0) + (safeSummary.malayalamOther || 0) %></td>
                    <td><%= safeTotalStudents %></td>
                </tr>
            </tbody>
        </table>

        <!-- QP Type by Medium -->
        <div class="section-title">QUESTION PAPER TYPE DISTRIBUTION</div>
        <table class="summary-table">
            <thead>
                <tr>
                    <th>Medium</th>
                    <th>QP Type A</th>
                    <th>QP Type B</th>
                    <th>Total</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td class="medium-label">English</td>
                    <td><%= safeSummary.englishTypeA || 0 %></td>
                    <td><%= safeSummary.englishTypeB || 0 %></td>
                    <td><%= safeSummary.totalEnglishStudents || 0 %></td>
                </tr>
                <tr>
                    <td class="medium-label">Malayalam</td>
                    <td><%= safeSummary.malayalamTypeA || 0 %></td>
                    <td><%= safeSummary.malayalamTypeB || 0 %></td>
                    <td><%= safeSummary.totalMalayalamStudents || 0 %></td>
                </tr>
                <tr class="total-row">
                    <td class="medium-label">TOTAL</td>
                    <td><%= safeSummary.totalTypeA || 0 %></td>
                    <td><%= safeSummary.totalTypeB || 0 %></td>
                    <td><%= safeTotalStudents %></td>
                </tr>
            </tbody>
        </table>

        <!-- Room Wise Distribution with QP Types -->
        <div class="section-title">ROOM WISE DISTRIBUTION</div>
        <table class="room-table">
            <thead>
                <tr>
                    <th rowspan="2">Room No</th>
                    <th rowspan="2">Total</th>
                    <th colspan="2">Medium</th>
                    <th colspan="2">Gender</th>
                    <!-- <th colspan="2">QP Type</th> -->
                    <th colspan="2">English QP</th>
                    <th colspan="2">Malayalam QP</th>
                </tr>
                <tr>
                    <th>Eng</th>
                    <th>Mal</th>
                    <th>M</th>
                    <th>F</th>
                    <!-- <th>O</th> -->
                    <!-- <th>A</th> -->
                    <!-- <th>B</th> -->
                    <th>A</th>
                    <th>B</th>
                    <th>A</th>
                    <th>B</th>
                </tr>
            </thead>
            <tbody>
                <% if (safeRooms && safeRooms.length > 0) { %>
                    <% safeRooms.forEach(room => { %>
                    <tr>
                        <td><strong><%= room.roomNo %></strong></td>
                        <td><%= room.total || 0 %></td>
                        <td><%= room.english || 0 %></td>
                        <td><%= room.malayalam || 0 %></td>
                        <td><%= room.male || 0 %></td>
                        <td><%= room.female || 0 %></td>
                        <!-- <td><%= room.other || 0 %></td> -->
                        <!-- <td><%= room.typeA || 0 %></td> -->
                        <!-- <td><%= room.typeB || 0 %></td> -->
                        <td><%= room.englishTypeA || 0 %></td>
                        <td><%= room.englishTypeB || 0 %></td>
                        <td><%= room.malayalamTypeA || 0 %></td>
                        <td><%= room.malayalamTypeB || 0 %></td>
                    </tr>
                    <% }) %>
                    <tr class="total-row">
                        <td><strong>TOTAL</strong></td>
                        <td><%= safeTotalStudents %></td>
                        <td><%= (safeSummary.totalEnglishStudents || 0) %></td>
                        <td><%= (safeSummary.totalMalayalamStudents || 0) %></td>
                        <td><%= (safeSummary.totalMale || 0) %></td>
                        <td><%= (safeSummary.totalFemale || 0) %></td>
                        <!-- <td><%= (safeSummary.totalOther || 0) %></td> -->
                        <!-- <td><%= (safeSummary.totalTypeA || 0) %></td> -->
                        <!-- <td><%= (safeSummary.totalTypeB || 0) %></td> -->
                        <td><%= (safeSummary.englishTypeA || 0) %></td>
                        <td><%= (safeSummary.englishTypeB || 0) %></td>
                        <td><%= (safeSummary.malayalamTypeA || 0) %></td>
                        <td><%= (safeSummary.malayalamTypeB || 0) %></td>
                    </tr>
                <% } else { %>
                    <tr>
                        <td colspan="13" style="text-align: center; padding: 5mm;">No room data available</td>
                    </tr>
                <% } %>
            </tbody>
        </table>

        <!-- Summary Notes -->
        <div style="margin-top: 5mm; font-size: 9pt;">
            <p><strong>Summary:</strong> Total <%= safeTotalStudents %> students across <%= safeTotalRooms %> rooms.</p>
            <p>English Medium: <%= safeSummary.totalEnglishStudents || 0 %> students (Male: <%= safeSummary.englishMale || 0 %>, Female: <%= safeSummary.englishFemale || 0 %>)</p>
            <p>Malayalam Medium: <%= safeSummary.totalMalayalamStudents || 0 %> students (Male: <%= safeSummary.malayalamMale || 0 %>, Female: <%= safeSummary.malayalamFemale || 0 %>)</p>
            <p>Question Papers Required: Type A - <%= safeSummary.totalTypeA || 0 %>, Type B - <%= safeSummary.totalTypeB || 0 %></p>
        </div>

        <!-- Footer -->
        <div class="footer-section">
            <div>PPMHSS KOTTUKKARA - NMEA TENDER SCHOLAR 26 - Overall Summary Report</div>
            <div style="margin-top: 1mm;">Generated on <%= safeGenerationDate %> at <%= safeGenerationTime %> | Page 1 of 1</div>
        </div>
    </div>

    <!-- Action Buttons -->
    <% if (typeof isPreview !== 'undefined' && isPreview) { %>
    <div class="action-buttons no-print">
        <button class="print-button" onclick="window.print()">🖨️ Print Summary Report</button>
        <a href="javascript:history.back()" class="back-button">← Back</a>
    </div>
    <% } %>

    <script>
        <% if (typeof autoPrint !== 'undefined' && autoPrint) { %>
        window.addEventListener('load', function() {
            setTimeout(function() {
                window.print();
            }, 500);
        });
        <% } %>
    </script>
</body>
</html>